<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta name="description" content="Ali Hassan Mobile Store – Premium mobile phones, accessories & repair services in Gujrat, Pakistan. Best prices on Samsung, iPhone, Infinix, Tecno, Oppo & Vivo.">
<meta name="keywords" content="mobile shop in Gujrat Pakistan, smartphone accessories Gujrat, mobile repair services Gujrat, buy iPhone Gujrat, Samsung Galaxy Gujrat, Infinix phones Pakistan, Tecno phones, Oppo Vivo Gujrat">
<meta name="author" content="Ali Hassan Mobile Store">
<meta property="og:title" content="Ali Hassan Mobile Store – Gujrat's #1 Mobile Shop">
<meta property="og:description" content="Latest smartphones, accessories & expert repair in Gujrat, Pakistan.">
<title>Ali Hassan Mobile Store | Smartphones, Accessories & Repair – Gujrat Pakistan</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=DM+Sans:ital,wght@0,300;0,400;0,500;1,400&display=swap" rel="stylesheet">
<style>
:root {
  --navy: #0a0f1e;
  --navy2: #111827;
  --electric: #00c6ff;
  --gold: #f5a623;
  --gold2: #ffd166;
  --white: #ffffff;
  --off: #f0f4ff;
  --muted: #94a3b8;
  --card: #151c2e;
  --card2: #1e2a42;
  --green: #22c55e;
  --red: #ef4444;
  --radius: 16px;
  --shadow: 0 8px 32px rgba(0,198,255,0.10);
  --trans: all 0.3s cubic-bezier(0.4,0,0.2,1);
}
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html { scroll-behavior: smooth; font-size: 16px; }
body {
  font-family: 'DM Sans', sans-serif;
  background: var(--navy);
  color: var(--white);
  overflow-x: hidden;
  line-height: 1.6;
}
h1,h2,h3,h4,h5 { font-family: 'Syne', sans-serif; line-height: 1.2; }

/* ─── SCROLLBAR ──────────────────────────── */
::-webkit-scrollbar { width: 6px; }
::-webkit-scrollbar-track { background: var(--navy2); }
::-webkit-scrollbar-thumb { background: var(--electric); border-radius: 3px; }

/* ─── TOPBAR ──────────────────────────────── */
.topbar {
  background: linear-gradient(90deg, var(--electric), var(--gold));
  color: var(--navy);
  text-align: center;
  font-size: 0.82rem;
  font-weight: 600;
  padding: 8px 16px;
  letter-spacing: 0.04em;
}
.topbar a { color: var(--navy); font-weight: 700; text-decoration: underline; }

/* ─── NAV ─────────────────────────────────── */
nav {
  position: sticky; top: 0; z-index: 999;
  background: rgba(10,15,30,0.95);
  backdrop-filter: blur(18px);
  border-bottom: 1px solid rgba(0,198,255,0.12);
  padding: 0 24px;
  display: flex; align-items: center; justify-content: space-between;
  height: 68px;
  transition: var(--trans);
}
.logo {
  display: flex; align-items: center; gap: 10px; text-decoration: none;
}
.logo-icon {
  width: 42px; height: 42px;
  background: linear-gradient(135deg, var(--electric), #0072ff);
  border-radius: 10px;
  display: flex; align-items: center; justify-content: center;
  font-size: 1.3rem;
}
.logo-text { font-family: 'Syne', sans-serif; font-weight: 800; font-size: 1.1rem; }
.logo-text span { color: var(--electric); }
.nav-links {
  display: flex; gap: 8px; list-style: none;
}
.nav-links a {
  color: var(--muted);
  text-decoration: none;
  font-size: 0.88rem;
  font-weight: 500;
  padding: 8px 12px;
  border-radius: 8px;
  transition: var(--trans);
  white-space: nowrap;
}
.nav-links a:hover { color: var(--white); background: rgba(0,198,255,0.1); }
.nav-links a.active { color: var(--electric); }
.nav-cta {
  background: linear-gradient(135deg, var(--electric), #0072ff);
  color: var(--white) !important;
  padding: 9px 18px !important;
  border-radius: 8px !important;
  font-weight: 600 !important;
}
.nav-cta:hover { transform: translateY(-1px); box-shadow: 0 6px 20px rgba(0,198,255,0.35) !important; }
.hamburger {
  display: none; flex-direction: column; gap: 5px; cursor: pointer;
  background: none; border: none; padding: 4px;
}
.hamburger span {
  display: block; width: 24px; height: 2px;
  background: var(--white); border-radius: 2px;
  transition: var(--trans);
}
.mobile-menu {
  display: none; position: fixed; top: 68px; left: 0; right: 0;
  background: rgba(10,15,30,0.98); backdrop-filter: blur(20px);
  border-bottom: 1px solid rgba(0,198,255,0.15);
  padding: 20px; z-index: 998;
  flex-direction: column; gap: 4px;
}
.mobile-menu.open { display: flex; }
.mobile-menu a {
  color: var(--muted); text-decoration: none; font-size: 1rem;
  padding: 12px 16px; border-radius: 10px;
  transition: var(--trans); font-weight: 500;
}
.mobile-menu a:hover { color: var(--white); background: rgba(0,198,255,0.1); }

/* ─── HERO ─────────────────────────────────── */
#home {
  min-height: 100vh;
  display: flex; align-items: center;
  position: relative; overflow: hidden;
  padding: 80px 24px 60px;
}
.hero-bg {
  position: absolute; inset: 0; z-index: 0;
  background:
    radial-gradient(ellipse 70% 60% at 70% 50%, rgba(0,114,255,0.18) 0%, transparent 65%),
    radial-gradient(ellipse 40% 40% at 20% 80%, rgba(245,166,35,0.10) 0%, transparent 60%),
    linear-gradient(160deg, #0a0f1e 0%, #111827 60%, #0d1829 100%);
}
.hero-grid {
  position: absolute; inset: 0; z-index: 0; opacity: 0.04;
  background-image: linear-gradient(var(--electric) 1px, transparent 1px),
    linear-gradient(90deg, var(--electric) 1px, transparent 1px);
  background-size: 60px 60px;
}
.hero-content {
  position: relative; z-index: 1;
  max-width: 1200px; margin: 0 auto; width: 100%;
  display: grid; grid-template-columns: 1fr 1fr;
  gap: 60px; align-items: center;
}
.hero-badge {
  display: inline-flex; align-items: center; gap: 8px;
  background: rgba(0,198,255,0.1);
  border: 1px solid rgba(0,198,255,0.25);
  border-radius: 50px; padding: 6px 16px 6px 8px;
  font-size: 0.82rem; font-weight: 600; color: var(--electric);
  margin-bottom: 20px;
}
.hero-badge .dot {
  width: 8px; height: 8px; border-radius: 50%;
  background: var(--green);
  animation: pulse 2s infinite;
}
@keyframes pulse {
  0%,100% { box-shadow: 0 0 0 0 rgba(34,197,94,0.6); }
  50% { box-shadow: 0 0 0 6px rgba(34,197,94,0); }
}
.hero-title {
  font-size: clamp(2.2rem, 5vw, 3.8rem);
  font-weight: 800; line-height: 1.1;
  margin-bottom: 20px;
}
.hero-title .accent { color: var(--electric); display: block; }
.hero-title .gold { color: var(--gold); }
.hero-sub {
  font-size: 1.1rem; color: var(--muted);
  margin-bottom: 32px; max-width: 480px; line-height: 1.7;
}
.hero-btns { display: flex; gap: 14px; flex-wrap: wrap; margin-bottom: 40px; }
.btn-primary {
  background: linear-gradient(135deg, var(--electric), #0072ff);
  color: var(--white); border: none; padding: 14px 28px;
  border-radius: 12px; font-size: 1rem; font-weight: 600;
  cursor: pointer; text-decoration: none; display: inline-flex;
  align-items: center; gap: 8px; transition: var(--trans);
  font-family: 'DM Sans', sans-serif;
}
.btn-primary:hover { transform: translateY(-2px); box-shadow: 0 10px 30px rgba(0,198,255,0.4); }
.btn-secondary {
  background: rgba(255,255,255,0.06);
  border: 1px solid rgba(255,255,255,0.15);
  color: var(--white); padding: 14px 28px;
  border-radius: 12px; font-size: 1rem; font-weight: 600;
  cursor: pointer; text-decoration: none; display: inline-flex;
  align-items: center; gap: 8px; transition: var(--trans);
  font-family: 'DM Sans', sans-serif;
}
.btn-secondary:hover { background: rgba(255,255,255,0.12); transform: translateY(-2px); }
.hero-stats {
  display: flex; gap: 32px; flex-wrap: wrap;
}
.hero-stat .num {
  font-family: 'Syne', sans-serif; font-size: 1.8rem;
  font-weight: 800; color: var(--electric);
}
.hero-stat .label { font-size: 0.82rem; color: var(--muted); font-weight: 500; }
.hero-visual {
  position: relative; display: flex;
  align-items: center; justify-content: center;
}
.phone-showcase {
  position: relative; width: 280px; height: 480px;
  background: linear-gradient(145deg, var(--card), var(--card2));
  border-radius: 38px;
  border: 1px solid rgba(0,198,255,0.2);
  box-shadow: 0 30px 80px rgba(0,0,0,0.5), 0 0 0 1px rgba(255,255,255,0.05);
  display: flex; align-items: center; justify-content: center;
  font-size: 5rem;
  overflow: hidden;
  animation: float 4s ease-in-out infinite;
}
@keyframes float {
  0%,100% { transform: translateY(0) rotate(-2deg); }
  50% { transform: translateY(-18px) rotate(2deg); }
}
.phone-showcase::before {
  content: '';
  position: absolute; top: 0; left: 50%; transform: translateX(-50%);
  width: 120px; height: 26px;
  background: var(--navy); border-radius: 0 0 18px 18px; z-index: 2;
}
.phone-brands {
  position: absolute; right: -40px; top: 50%; transform: translateY(-50%);
  display: flex; flex-direction: column; gap: 12px;
}
.brand-pill {
  background: var(--card2);
  border: 1px solid rgba(0,198,255,0.15);
  border-radius: 50px; padding: 8px 16px;
  font-size: 0.8rem; font-weight: 600; color: var(--muted);
  white-space: nowrap; transition: var(--trans);
  animation: slideIn 0.5s ease both;
}
.brand-pill:hover { color: var(--electric); border-color: var(--electric); }
@keyframes slideIn {
  from { opacity: 0; transform: translateX(20px); }
  to { opacity: 1; transform: translateX(0); }
}
.brand-pill:nth-child(1) { animation-delay: 0.2s; }
.brand-pill:nth-child(2) { animation-delay: 0.4s; }
.brand-pill:nth-child(3) { animation-delay: 0.6s; }
.brand-pill:nth-child(4) { animation-delay: 0.8s; }
.brand-pill:nth-child(5) { animation-delay: 1.0s; }
.brand-pill:nth-child(6) { animation-delay: 1.2s; }

/* ─── SECTION COMMONS ────────────────────── */
section { padding: 80px 24px; }
.container { max-width: 1200px; margin: 0 auto; width: 100%; }
.section-label {
  display: inline-block;
  font-size: 0.78rem; font-weight: 700; letter-spacing: 0.1em;
  text-transform: uppercase; color: var(--electric);
  background: rgba(0,198,255,0.1);
  border: 1px solid rgba(0,198,255,0.2);
  padding: 4px 14px; border-radius: 50px;
  margin-bottom: 14px;
}
.section-title {
  font-size: clamp(1.8rem, 3.5vw, 2.8rem);
  font-weight: 800; margin-bottom: 14px;
}
.section-title span { color: var(--electric); }
.section-sub { color: var(--muted); max-width: 560px; font-size: 1rem; line-height: 1.7; }
.section-header { margin-bottom: 48px; }

/* ─── TRUST BADGES ───────────────────────── */
.trust-strip {
  background: var(--card);
  border-top: 1px solid rgba(0,198,255,0.08);
  border-bottom: 1px solid rgba(0,198,255,0.08);
  padding: 28px 24px;
}
.trust-inner {
  max-width: 1200px; margin: 0 auto;
  display: flex; align-items: center; justify-content: space-around;
  flex-wrap: wrap; gap: 24px;
}
.trust-item {
  display: flex; align-items: center; gap: 12px;
  color: var(--muted); font-size: 0.9rem; font-weight: 500;
}
.trust-icon {
  width: 44px; height: 44px;
  background: rgba(0,198,255,0.1); border: 1px solid rgba(0,198,255,0.2);
  border-radius: 10px; display: flex; align-items: center;
  justify-content: center; font-size: 1.2rem; flex-shrink: 0;
}
.trust-item strong { color: var(--white); display: block; font-size: 0.95rem; }

/* ─── ABOUT ──────────────────────────────── */
#about { background: var(--navy2); }
.about-grid {
  display: grid; grid-template-columns: 1fr 1fr; gap: 60px; align-items: center;
}
.about-img {
  position: relative; border-radius: var(--radius);
  overflow: hidden; background: var(--card);
  height: 440px; display: flex; align-items: center;
  justify-content: center; font-size: 6rem;
  border: 1px solid rgba(0,198,255,0.1);
}
.about-img::before {
  content: '';
  position: absolute; inset: 0;
  background: linear-gradient(135deg, rgba(0,198,255,0.05), rgba(0,114,255,0.05));
}
.about-store-cards {
  position: absolute; bottom: 20px; left: 20px; right: 20px;
  display: grid; grid-template-columns: 1fr 1fr; gap: 12px;
}
.mini-card {
  background: rgba(10,15,30,0.85); backdrop-filter: blur(10px);
  border: 1px solid rgba(0,198,255,0.2);
  border-radius: 10px; padding: 12px 16px; text-align: center;
}
.mini-card .num { font-family: 'Syne', sans-serif; font-size: 1.5rem; font-weight: 800; color: var(--electric); }
.mini-card .lbl { font-size: 0.75rem; color: var(--muted); }
.about-features { display: flex; flex-direction: column; gap: 16px; margin-top: 28px; }
.about-feat {
  display: flex; gap: 16px; align-items: flex-start;
  background: var(--card); border: 1px solid rgba(255,255,255,0.05);
  border-radius: 12px; padding: 16px 20px; transition: var(--trans);
}
.about-feat:hover { border-color: rgba(0,198,255,0.2); transform: translateX(4px); }
.feat-icon {
  width: 40px; height: 40px; border-radius: 10px; flex-shrink: 0;
  background: linear-gradient(135deg, var(--electric), #0072ff);
  display: flex; align-items: center; justify-content: center; font-size: 1.1rem;
}
.feat-text strong { display: block; font-weight: 600; margin-bottom: 3px; }
.feat-text span { font-size: 0.88rem; color: var(--muted); }

/* ─── BRANDS ─────────────────────────────── */
#brands { padding: 60px 24px; }
.brands-row {
  display: flex; gap: 20px; align-items: center;
  justify-content: center; flex-wrap: wrap;
}
.brand-card {
  background: var(--card); border: 1px solid rgba(255,255,255,0.06);
  border-radius: 14px; padding: 20px 32px;
  font-family: 'Syne', sans-serif; font-weight: 700;
  font-size: 1.1rem; color: var(--muted);
  transition: var(--trans); cursor: default;
  letter-spacing: 0.03em;
}
.brand-card:hover {
  color: var(--white); border-color: var(--electric);
  background: var(--card2); transform: translateY(-4px);
  box-shadow: 0 12px 30px rgba(0,198,255,0.15);
}

/* ─── SMARTPHONES ────────────────────────── */
#smartphones { background: var(--navy2); }
.filter-tabs {
  display: flex; gap: 10px; flex-wrap: wrap; margin-bottom: 36px;
}
.tab-btn {
  background: var(--card); border: 1px solid rgba(255,255,255,0.08);
  color: var(--muted); padding: 8px 20px; border-radius: 50px;
  font-size: 0.88rem; font-weight: 600; cursor: pointer;
  transition: var(--trans); font-family: 'DM Sans', sans-serif;
}
.tab-btn.active, .tab-btn:hover {
  background: rgba(0,198,255,0.12);
  border-color: var(--electric); color: var(--electric);
}
.phones-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
  gap: 20px;
}
.phone-card {
  background: var(--card); border: 1px solid rgba(255,255,255,0.06);
  border-radius: 18px; overflow: hidden;
  transition: var(--trans); cursor: pointer;
  display: flex; flex-direction: column;
}
.phone-card:hover {
  transform: translateY(-6px);
  border-color: rgba(0,198,255,0.25);
  box-shadow: 0 20px 50px rgba(0,0,0,0.4);
}
.phone-img {
  background: linear-gradient(135deg, var(--card2), rgba(0,198,255,0.05));
  height: 200px; display: flex; align-items: center;
  justify-content: center; font-size: 5rem; position: relative;
}
.phone-badge {
  position: absolute; top: 12px; left: 12px;
  background: var(--electric); color: var(--navy);
  font-size: 0.72rem; font-weight: 700; padding: 3px 10px;
  border-radius: 50px; letter-spacing: 0.05em;
}
.phone-badge.offer { background: var(--gold); }
.phone-badge.new { background: var(--green); }
.phone-info { padding: 18px; flex: 1; display: flex; flex-direction: column; }
.phone-brand { font-size: 0.75rem; color: var(--electric); font-weight: 600; text-transform: uppercase; letter-spacing: 0.08em; margin-bottom: 4px; }
.phone-name { font-family: 'Syne', sans-serif; font-size: 1rem; font-weight: 700; margin-bottom: 8px; }
.phone-specs { display: flex; gap: 8px; flex-wrap: wrap; margin-bottom: 14px; }
.spec-tag {
  background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.08);
  color: var(--muted); font-size: 0.72rem; padding: 3px 8px; border-radius: 6px;
}
.phone-price { display: flex; align-items: baseline; gap: 10px; margin-bottom: 14px; margin-top: auto; }
.price-main { font-family: 'Syne', sans-serif; font-size: 1.3rem; font-weight: 800; color: var(--gold); }
.price-old { font-size: 0.85rem; color: var(--muted); text-decoration: line-through; }
.phone-btns { display: flex; gap: 10px; }
.btn-buy {
  flex: 1; background: linear-gradient(135deg, var(--electric), #0072ff);
  color: var(--white); border: none; padding: 10px;
  border-radius: 10px; font-size: 0.88rem; font-weight: 600;
  cursor: pointer; transition: var(--trans); font-family: 'DM Sans', sans-serif;
  text-align: center; text-decoration: none; display: flex;
  align-items: center; justify-content: center;
}
.btn-buy:hover { transform: translateY(-1px); box-shadow: 0 6px 18px rgba(0,198,255,0.35); }
.btn-wa {
  background: rgba(34,197,94,0.15); border: 1px solid rgba(34,197,94,0.25);
  color: var(--green); padding: 10px 14px; border-radius: 10px;
  font-size: 1.1rem; cursor: pointer; transition: var(--trans);
  text-decoration: none; display: flex; align-items: center; justify-content: center;
}
.btn-wa:hover { background: rgba(34,197,94,0.25); }

/* ─── ACCESSORIES ────────────────────────── */
#accessories {}
.acc-grid {
  display: grid; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
  gap: 16px;
}
.acc-card {
  background: var(--card); border: 1px solid rgba(255,255,255,0.06);
  border-radius: 14px; padding: 24px 20px;
  text-align: center; transition: var(--trans); cursor: pointer;
}
.acc-card:hover {
  border-color: rgba(245,166,35,0.3); transform: translateY(-4px);
  box-shadow: 0 12px 30px rgba(245,166,35,0.1);
}
.acc-icon { font-size: 2.5rem; margin-bottom: 12px; }
.acc-name { font-family: 'Syne', sans-serif; font-weight: 700; margin-bottom: 6px; }
.acc-desc { font-size: 0.82rem; color: var(--muted); margin-bottom: 14px; line-height: 1.5; }
.acc-price { color: var(--gold); font-weight: 700; font-size: 1.05rem; }

/* ─── REPAIR SERVICES ────────────────────── */
#repair { background: var(--navy2); }
.repair-grid {
  display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 20px;
}
.repair-card {
  background: var(--card); border: 1px solid rgba(255,255,255,0.06);
  border-radius: 18px; padding: 28px 24px;
  transition: var(--trans); position: relative; overflow: hidden;
}
.repair-card::before {
  content: ''; position: absolute; top: 0; left: 0; right: 0; height: 3px;
  background: linear-gradient(90deg, var(--electric), #0072ff);
  transform: scaleX(0); transition: var(--trans); transform-origin: left;
}
.repair-card:hover::before { transform: scaleX(1); }
.repair-card:hover { border-color: rgba(0,198,255,0.2); transform: translateY(-4px); }
.repair-icon { font-size: 2rem; margin-bottom: 14px; }
.repair-name { font-family: 'Syne', sans-serif; font-size: 1.1rem; font-weight: 700; margin-bottom: 8px; }
.repair-desc { color: var(--muted); font-size: 0.88rem; margin-bottom: 18px; line-height: 1.6; }
.repair-meta { display: flex; justify-content: space-between; align-items: center; }
.repair-time { font-size: 0.8rem; color: var(--muted); }
.repair-time strong { color: var(--green); }
.repair-price { font-weight: 700; color: var(--electric); }
.repair-cta {
  margin-top: 48px; text-align: center;
  background: var(--card2); border: 1px solid rgba(0,198,255,0.15);
  border-radius: 20px; padding: 40px;
}
.repair-cta h3 { font-size: 1.6rem; margin-bottom: 10px; }
.repair-cta p { color: var(--muted); margin-bottom: 24px; }
.repair-btns { display: flex; gap: 14px; justify-content: center; flex-wrap: wrap; }
.btn-call {
  background: var(--gold); color: var(--navy); padding: 13px 28px;
  border-radius: 12px; font-weight: 700; cursor: pointer; border: none;
  font-family: 'DM Sans', sans-serif; font-size: 0.95rem;
  text-decoration: none; display: inline-flex; align-items: center; gap: 8px;
  transition: var(--trans);
}
.btn-call:hover { background: var(--gold2); transform: translateY(-2px); }
.btn-wa-big {
  background: rgba(34,197,94,0.15); border: 1.5px solid var(--green);
  color: var(--green); padding: 13px 28px;
  border-radius: 12px; font-weight: 700; cursor: pointer;
  font-family: 'DM Sans', sans-serif; font-size: 0.95rem;
  text-decoration: none; di
